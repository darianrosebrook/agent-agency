# Grafana dashboard configuration for Multimodal RAG
apiVersion: 1

providers:
  - name: 'Multimodal RAG Dashboards'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
